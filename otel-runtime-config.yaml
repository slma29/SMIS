receivers:
  prometheus:
    config:
      scrape_configs:
        - job_name: node-exporter
          scrape_interval: 10s
          static_configs:
            - targets: ['node-exporter:9100']

processors:
  resource:
    attributes:
      - key: repo
        value: "slma29/SMIS"
        action: upsert
      - key: token
        value: "${env:SENTINEL_TOKEN}"
        action: upsert
      - key: version
        value: "${env:REPO_VERSION}"
        action: upsert
      - key: correlation_id
        value: "slma29/SMIS|${env:REPO_VERSION}|${env:SENTINEL_TOKEN}"
        action: upsert

  transform:
    metric_statements:
      - context: datapoint
        statements:
          - set(attributes["repo"], resource.attributes["repo"])
          - set(attributes["token"], resource.attributes["token"])
          - set(attributes["version"], resource.attributes["version"])
          - set(attributes["correlation_id"], resource.attributes["correlation_id"])

  batch:
    timeout: 5s

exporters:
  otlphttp/metrics:
    endpoint: "https://dsc-actually-vary-ensuring.trycloudflare.com"
    headers:
      X-Sentinel-Token: "${env:SENTINEL_TOKEN}"
      X-Sentinel-Secret: "${env:SENTINEL_SECRET}"

service:
  pipelines:
    metrics:
      receivers: [prometheus]
      processors: [resource, transform, batch]
      exporters: [otlphttp/metrics]
